# Dependencias usadas apenas para gerar a documentacao do codigo-fonte do
# backend (mkdocs + mkdocstrings le as docstrings dos modulos em app/).
# Nao entram na imagem de runtime nem em requirements.txt. Instale com:
#   pip install -r backend/requirements-docs.txt
# e gere o site com:
#   mkdocs build      (site estatico em site/)
#   mkdocs serve      (preview em http://127.0.0.1:8000)
mkdocs==1.6.1
mkdocs-material==9.7.7
mkdocstrings==1.0.6
mkdocstrings-python==2.0.7
mkdocs-gen-files==0.6.1
mkdocs-literate-nav==0.6.3
mkdocs-section-index==0.3.12
ruff==0.16.4
